# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper
from mergedapi.models.metadata import Metadata
from mergedapi.models.spec import Spec
from mergedapi.models.status import Status


class StatusNodeItem(object):

    """Implementation of the 'StatusNodeItem' model.

    TODO: type model description here.

    Attributes:
        metadata (Metadata): TODO: type description here.
        spec (Spec): TODO: type description here.
        status (Status): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "metadata": 'metadata',
        "spec": 'spec',
        "status": 'status'
    }

    _optionals = [
        'metadata',
        'spec',
        'status',
    ]

    def __init__(self,
                 metadata=APIHelper.SKIP,
                 spec=APIHelper.SKIP,
                 status=APIHelper.SKIP):
        """Constructor for the StatusNodeItem class"""

        # Initialize members of the class
        if metadata is not APIHelper.SKIP:
            self.metadata = metadata 
        if spec is not APIHelper.SKIP:
            self.spec = spec 
        if status is not APIHelper.SKIP:
            self.status = status 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        metadata = Metadata.from_dictionary(dictionary.get('metadata')) if 'metadata' in dictionary.keys() else APIHelper.SKIP
        spec = Spec.from_dictionary(dictionary.get('spec')) if 'spec' in dictionary.keys() else APIHelper.SKIP
        status = Status.from_dictionary(dictionary.get('status')) if 'status' in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(metadata,
                   spec,
                   status)
