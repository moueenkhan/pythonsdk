# -*- coding: utf-8 -*-

"""
mergedapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mergedapi.api_helper import APIHelper
from mergedapi.models.triggervalue_chunk import TriggervalueChunk


class TriggerValueResponse(object):

    """Implementation of the 'TriggerValueResponse' model.

    Trigger Details

    Attributes:
        triggers (list of TriggervalueChunk): Trigger value chunk details.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "triggers": 'triggers'
    }

    _optionals = [
        'triggers',
    ]

    def __init__(self,
                 triggers=APIHelper.SKIP):
        """Constructor for the TriggerValueResponse class"""

        # Initialize members of the class
        if triggers is not APIHelper.SKIP:
            self.triggers = triggers 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        triggers = None
        if dictionary.get('triggers') is not None:
            triggers = [TriggervalueChunk.from_dictionary(x) for x in dictionary.get('triggers')]
        else:
            triggers = APIHelper.SKIP
        # Return an object of this model
        return cls(triggers)
